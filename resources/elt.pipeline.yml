resources:
  pipelines:
    etl_pipeline:
      name: etl_pipeline
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true

      libraries:
        - glob:
            include: ../src/pipeline_etl/transformations/**

      configuration:
        source_catalog: ${var.catalog}
        source_schema: ${var.schema}